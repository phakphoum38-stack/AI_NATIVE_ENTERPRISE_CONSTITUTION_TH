name: ai_native_enterprise_constitution
version: 0.1.0+1
publish_to: none
description: AI-native enterprise constitution and provider foundation.

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
